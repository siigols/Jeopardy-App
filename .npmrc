# NODE_ENV=production makes npm skip devDependencies, but the build tooling
# (vite, typescript, @vitejs/plugin-react, @types/*) lives there and `npm run
# build` needs it. Forcing include=dev here means the install is correct even
# if the platform's build command is just `npm ci` / `npm install`.
include=dev
